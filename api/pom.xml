<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.eventbooking</groupId>
    <artifactId>event-booking-api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>event-booking-api</name>
    <description>Event Booking System API (Cambodia)</description>

    <properties>
        <java.version>21</java.version>
        <!-- Stated explicitly as well as on maven-compiler-plugin below. Maven
             needs neither - it takes the level from java.version via the Spring
             Boot parent - but m2e, inside the VS Code Java language server, reads
             these properties when it cannot work the level out from a compiler
             mojo execution, which this pom disables so kapt can run first.
             Without them it silently assumes 1.8 and every record and text block
             in the codebase lights up as a syntax error. -->
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <jjwt.version>0.12.6</jjwt.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <!-- springdoc 3.x is the Spring Boot 4 / Spring Framework 7 line.
             The 2.x releases target Boot 3 and will not start here. -->
        <springdoc.version>3.0.2</springdoc.version>
        <zxing.version>3.5.3</zxing.version>
        <cloudinary.version>2.3.0</cloudinary.version>
        <!-- Not managed by the Boot BOM, so the version is pinned here. -->
        <commons-io.version>2.22.0</commons-io.version>
    <kotlin.version>2.3.10</kotlin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Testcontainers BOM: keeps testcontainers-* and junit-jupiter module
                 versions aligned so they don't drift independently. -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Web / REST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA / Hibernate -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- OpenAPI 3 + Swagger UI at /swagger-ui.html, generated from the
             controllers themselves so the documentation cannot drift from the
             endpoints. Also supplies the io.swagger.v3 annotations the DTOs
             use to describe their fields. -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- QR encoding for tickets. `core` only, deliberately not `javase`:
             that module renders through java.awt/ImageIO, which drags a headful
             graphics stack into a server that has no business having one.
             QrRenderer turns ZXing's BitMatrix into SVG instead - crisp at any
             gate scanner's zoom, and a few hundred bytes. -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>${zxing.version}</version>
        </dependency>

        <!-- Validation (@Valid, @NotNull, etc.) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- PostgreSQL driver. Compile-scoped, not runtime:
             DatabaseExceptionTranslator imports PSQLException and
             ServerErrorMessage directly to read SQLSTATE and constraint names
             off a failed write, so the driver has to be on the compile
             classpath. With scope=runtime the project does not build. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <!-- Flyway: runs db/migration/V*.sql in order, tracks what already ran.
             flyway-database-postgresql is required separately since Flyway 10.
             spring-boot-flyway carries the auto-configuration that actually
             triggers migration on startup - in Boot 4 it lives in its own
             module instead of spring-boot-autoconfigure, so flyway-core alone
             puts Flyway on the classpath but nothing ever runs it. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Lombok (less boilerplate) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- commons-io: FilenameUtils, for pulling the extension off an
             uploaded filename before it is handed to Cloudinary. -->
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>

        <!-- Cloudinary: hosts the event and avatar images whose public ids are
             stored in Event.cloudinaryImageId / AppUser.cloudinaryImageId.
             The -http5 artifact is the one built on Apache HttpClient 5, which
             is what Boot 4 manages; the older -http44 line drags in HttpClient
             4 alongside it. -->
        <dependency>
            <groupId>com.cloudinary</groupId>
            <artifactId>cloudinary-http5</artifactId>
            <version>${cloudinary.version}</version>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>

        <!-- Actuator: exposes /actuator/health, /actuator/prometheus, etc. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Micrometer → Prometheus bridge: formats all Spring Boot metrics
             into Prometheus exposition format at /actuator/prometheus so
             the Prometheus container can scrape them. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Testcontainers: spins up a real Postgres in Docker per test run,
             so plpgsql triggers, partial indexes and CHECK constraints from
             V1__schema.sql are actually exercised. H2 can't run any of those,
             which is why it was dropped from the runtime deps above. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib-jdk8</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-test</artifactId>
            <version>${kotlin.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>



    <build>
        <plugins>
            <!-- Surefire's default includes are *Test / Test* / *Tests / *TestCase.
                 They match none of our *IT.java files, so BookingCheckoutIT and
                 HoldConcurrencyIT had never executed once - the concurrency test
                 guarding against double-booking least of all. Failsafe binds them
                 to integration-test/verify, which is why CI runs `verify` and not
                 `test`. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration/>
                    </execution>
                    <execution>
                        <id>test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>test-compile</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-noarg</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-allopen</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <jvmTarget>1.8</jvmTarget>
                    <compilerPlugins>
                        <plugin>jpa</plugin>
                        <plugin>all-open</plugin>
                        <plugin>spring</plugin>
                    </compilerPlugins>
                    <pluginOptions>
                        <option>all-open:annotation=javax.persistence.Entity</option>
                        <option>all-open:annotation=javax.persistence.Embeddable</option>
                        <option>all-open:annotation=javax.persistence.MappedSuperclass</option>
                        <option>all-open:annotation=jakarta.persistence.Entity</option>
                        <option>all-open:annotation=jakarta.persistence.Embeddable</option>
                        <option>all-open:annotation=jakarta.persistence.MappedSuperclass</option>
                    </pluginOptions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <!-- The stock default-compile / default-testCompile executions are
                     left in place deliberately.

                     They used to be disabled (<phase>none</phase>) with duplicates
                     bound by hand, which is the arrangement a mixed Kotlin/Java
                     build needs so kapt runs before javac. There are no Kotlin
                     sources in this project - every kotlin-maven-plugin goal
                     reports "No sources to compile" - so that bought nothing and
                     cost a great deal: m2e, the Maven support inside the VS Code
                     Java language server, reads the compliance level off the
                     default-compile execution. With it disabled m2e fell back to
                     its own default of 1.8, and at that level every record, text
                     block and pattern-match here reads as a syntax error - errors
                     in nearly every file, while Maven built perfectly.

                     Restore the disabled form only alongside actual Kotlin
                     sources, and expect the IDE to need java.configuration.runtimes
                     and an explicit release again if you do. -->
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
